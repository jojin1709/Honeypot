# ============================================================
# Honeypot Lab - Python Dependencies
# ============================================================
# NOTE: Only Cowrie (SSH/Telnet) is a third-party honeypot engine.
# All other 16 honeypots (ICS, credentials, web, DB, malware, RDP,
# SMB, DNS, SIP, Redis, VNC, Telnet, Memcached, MQTT, SNMP, NTP)
# are custom, dependency-free socket-level Python implementations
# built specifically for cross-platform (Windows + Linux) use.
# conpot/heralding are NOT required and are intentionally omitted.

# SSH/Telnet honeypot engine
cowrie>=3.0.0
twisted>=24.0.0                  # Required by Cowrie; also runs it via twistd

# Web framework (dashboard only)
flask>=3.0.0

# Used by the red-team test tools (SSH brute-force / scan checks)
paramiko>=3.0.0
